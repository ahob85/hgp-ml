// UI variables
let canvasDiv;
let canvas;
let textDiv;
let textP;

// Gameplay variables
let snake;
let resolution = 20;
let food;
let w;
let h;
let score = 0;

function setup() {
  // Setup the canvas
  canvasDiv = createDiv();
  canvas = createCanvas(640, 480);
  canvas.parent(canvasDiv);
  
}

function foodLocation() {

}

function draw() {
  // Refresh the background

  // Draw the snake

  // Draw the food

}

function keyPressed() {

}
